# Kubernetes Deployment (Legacy Index)

The maintained Kubernetes deployment documentation lives here:

- `packages/docs/deployment/kubernetes.md`
- `kubernetes/deploy.yaml`

Use the package docs as the source of truth.
